# Canvas CLI

Command-line interface for ingesting documents and generating CanvasPack artifacts.

## Usage

```bash
canvas ingest ./examples/sample.pdf --out pack.json
canvas runbook pack.json --out runbook.md
```

See the main `oss/README.md` for the full quickstart.
